document.addEventListener("DOMContentLoaded", function () {
    const loginform = document.getElementById("loginform");

    // Set up key navigation on both forms
    setupArrowNavigation(loginform)
});
